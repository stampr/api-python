language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
# command to install dependencies
install: 
    - "pip install . --use-mirrors"
    - "pip install pytest"
    - "pip install flexmock"
# command to run tests
script: py.test